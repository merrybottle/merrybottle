import React from 'react';
import styled from 'styled-components';

export const Controls: React.FC = () => {
  return <StyledControlsContainer />;
};

const StyledControlsContainer = styled.div`
  height: 80px;
`;
